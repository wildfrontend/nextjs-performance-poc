import { Stack } from '@mui/material';

import TextOnlyList from './basic';

const ListExample: React.FC = () => {
  return (
    <Stack spacing="16px">
      <TextOnlyList />
    </Stack>
  );
};

export default ListExample;
